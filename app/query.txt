FROM TABLE ROOM GET AVG(AREA) AND AVG(CAPACITY)
AND THEN FROM BUILDING GET BUILDINGNAME
AND THEN
JOIN ROOM WITH BUILDING ON ROOM.BUILDINGID = BUILDING.ID
GROUP AUTOMATICALLY
HAVING AVG(ROOM.AREA) > 1000
